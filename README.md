# swift-threadsafe-macro
Swift 6 macro for thread-safe reading and writing of a variable in a Sendable class. Without the @unchecked attribute, meaning that compiler checks for concurrency are preserved.
